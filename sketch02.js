//sketch 01
function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(255, 255, 255);

  stroke(255, 178, 102)
  strokeWeight(2)
  fill(255, 255, 204)
  ellipse(mouseX, mouseY, 120, 120)
}